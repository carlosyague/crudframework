package org.librae.common.facade;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

/**
 * Esta clase pretende incluir el comportamiento genérico de todo Facade.
 * Actualmente no tiene nada, en cuanto sea necesario incluir algo, este es el
 * lugar indicado.
 * 
 * @author cyague
 */
public class GenericFacadeImpl {

    /**
     * Log variable for all child classes. Uses LogFactory.getLog(getClass())
     * from Commons Logging.
     */
    protected final Log log = LogFactory.getLog(this.getClass());

}
